version: "3.8"
services:
  http_proxy:
    build: /home/lyall/Servers/HTTP-Reverse-Proxy
    container_name: http_proxy
    restart: unless-stopped
    network_mode: host
    volumes:
      - /home/lyall/Servers/HTTP-Reverse-Proxy:/data
